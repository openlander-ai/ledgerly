FROM node:22-alpine
WORKDIR /app
COPY package.json ./
RUN npm install --omit=dev
RUN echo "intentional mid-build failure (safety-gate)" && exit 1
COPY server.js ./
EXPOSE 3000
CMD ["node", "server.js"]
